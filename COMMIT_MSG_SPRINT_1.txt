feat(sprint-1): listing rico + api de búsqueda con filtros (Gaps #1, #2)

Backend Sprint 1 según SPRINT_PLAN.md:

Migraciones nuevas (database/migrations/):
- 1777700000001_create_subscription_plans_table (Free/Pro/Premium).
- 1777700000002_alter_users_add_publisher_fields (slug, bio, photo_url,
  verification_status, subscription_plan_id, parent_id, etc.).
- 1777700000003_create_states_table (32 estados INEGI).
- 1777700000004_create_municipalities_table (~40 municipios cubriendo
  8 estados; el resto se difiere a Sprint 4).
- 1777700000005_create_amenities_table (catálogo en español MX).
- 1777700000006_alter_listings_full_redesign (operation_type, property_type,
  beds, baths, parking, m2_built, m2_land, age, amenities json, state,
  municipality, colony, agent_id FK, video_url, virtual_tour_url,
  published_at, view_count, is_featured) con backfill.
- 1777700000007_add_indexes_listings (5 índices compuestos).

Endpoint GET /api/listings reescrito con VineJS validator + filtros:
operation, type, beds_min, baths_min, parking_min, min_price, max_price,
m2_built_min, m2_land_min, state, municipality, amenities CSV, q, bbox,
page, per_page, sort, is_featured. Devuelve {data, meta}.

Endpoints públicos nuevos: GET /api/states,
GET /api/states/:id/municipalities, GET /api/amenities (catalogs_controller).

Modelos: User extendido + Listing extendido + 4 nuevos (SubscriptionPlan,
State, Municipality, Amenity).

Seeders 01-06: planes, 32 estados, ~40 municipios, 31 amenidades, 6 agentes
ficticios y los 30 listings (database/seeds/listings_seed.json).

Tests funcionales:
- tests/functional/listings_search.spec.ts (10 casos cubriendo DoD).
- tests/functional/catalogs.spec.ts (5 casos).

Verificación manual: database/sql/verify_sprint_1.sql.

Pendiente humano: migration:fresh --seed + npm test + autocannon.

Cierra Gap #1 (Modelo Listing rico) y Gap #2 (API búsqueda con filtros).
Avanza parcial Gap #6 (RLS suave en update endpoint).
